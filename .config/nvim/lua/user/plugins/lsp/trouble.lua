return {
  "folke/trouble.nvim",
  opts = {
    auto_preview = false,
    auto_open = false,
    auto_close = false,
    focus = true, -- focus the window when opened
    open_no_results = true,
  },
  cmd = "Trouble",
  keys = {},
}
